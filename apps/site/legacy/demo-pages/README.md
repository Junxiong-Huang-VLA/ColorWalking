# Legacy Demo Pages

This folder keeps secondary presentation pages that were removed from the external primary flow in Round 6 closure:

- `product`
- `device`
- `progress`
- `story`
- `assets`

These pages are intentionally outside `src/` so they are not part of normal product builds.
If needed later, migrate them back as a dedicated demo-only bundle/module.
