# Legacy Non-Main Pages

These pages were physically demoted out of the runtime route map in final closure pass.

- `BrandManualPage.tsx`
- `CompanionPlushPage.tsx`

If needed for internal demo mode, migrate them into a dedicated demo bundle instead of app main routes.
